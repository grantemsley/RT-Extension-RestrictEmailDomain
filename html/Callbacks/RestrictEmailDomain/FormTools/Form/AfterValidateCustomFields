<%init>
return unless RT::Extension::RestrictEmailDomain->enabled;

my $args    = $ARGSRef;
my @errors  = RT::Extension::RestrictEmailDomain->ValidateEmailAddresses($args);

if (@errors) {
    push @$results, @errors;
}
</%init>

<%args>
$ARGSRef => undef
$results => undef
</%args>

